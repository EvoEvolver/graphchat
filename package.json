{
  "name": "graphchat-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --recursive build"
  },
  "workspaces": [
    "packages/*"
  ]
}